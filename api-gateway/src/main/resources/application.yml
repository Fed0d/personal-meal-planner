server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: auth-service
              uri: http://localhost:8081
              predicates:
                - Path=/api/v1/auth/**

            - id: user-service
              uri: http://localhost:8082
              predicates:
                - Path=/api/v1/users/**

            - id: meal-service
              uri: http://localhost:8083
              predicates:
                - Path=/api/v1/dishes/**

management:
  endpoints:
    web:
      exposure:
        include: health,info
